name: 035_phenobase
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - mypy
  - ruff
  - pytest
  - pytest-cov
  - pytest-integration
  - pip
  - python-dotenv
  - psycopg2
  - sqlalchemy
  - types-psycopg2
  - fastapi
  - uvicorn
  - httpx
  - mlflow
  - paramiko
  - types-paramiko
  - pip: 
  # pip-only packages go here if needed

     name: 035_phenobase
     channels:
       - conda-forge
       - defaults
     dependencies:
       - python=3.12
       - autopep8
       - mypy
       - pylint
       - pytest
       - pytest-cov
       - python-dotenv
       - smbprotocol
       - pip
       - pip:
         # pip-only packages go here if needed
         