     name: 035_phenobase
     channels:
       - conda-forge
       - defaults
     dependencies:
       - python=3.12
       - mypy
       - ruff
       - conda-audit
       - pytest
       - pytest-cov
       - pytest-integration
       - pip
       - python-dotenv
       - psycopg2
       - types-psycopg2
       - sqlalchemy
       - pip:
         # pip-only packages go here if needed